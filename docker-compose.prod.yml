services:
  db:
    image: pgvector/pgvector:pg16
    container_name: ptcg_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?set POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:?set POSTGRES_DB}
    volumes:
      - ./data/pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ptcg-internal

  ptcg-web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: ptcg_web
    restart: unless-stopped
    env_file:
      - ${ENV_FILE:-.env.production}
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      FLASK_DEBUG: "false"
      # 強制要求密鑰存在，缺漏時 compose 直接失敗（fail-closed）
      SECRET_KEY: ${SECRET_KEY:?set SECRET_KEY in .env.production}
      SECURITY_PASSWORD_SALT: ${SECURITY_PASSWORD_SALT:?set SECURITY_PASSWORD_SALT in .env.production}
      ENABLE_JP_DECK_AUTO_UPDATE: ${ENABLE_JP_DECK_AUTO_UPDATE:-false}
      ENABLE_LIMITLESS_AUTO_UPDATE: ${ENABLE_LIMITLESS_AUTO_UPDATE:-false}
      PYTHONUNBUFFERED: "1"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./data:/app/data
    expose:
      - "5000"
    networks:
      - ptcg-internal
      - proxy

networks:
  ptcg-internal:
    driver: bridge
  proxy:
    external: true
